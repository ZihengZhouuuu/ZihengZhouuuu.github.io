# 2026-09-05

## index.html：首屏新增 h2 大标题「空间智能与人机协同工作流设计者」
- 结构：`.compass-stage` 顶部原单个 `.portfolio-thesis` 升级为 `.hero-intro` 容器
  （h2.hero-role + p.portfolio-thesis 竖排居中）。
- 排版：hero-role clamp(24px, 2.8vw, 42px)/950/-0.05em；定位从 thesis 移到 hero-intro
  （top clamp(56px,7.5vh,92px)），thesis 变为容器内流式子元素。
- **三重压缩陷阱（踩坑实录）**：
  1. 首版文字块压罗盘弧顶（overlap 28px）→ 依次调 top/gap/字号；
  2. 改根定义 --compass-size 的 70vh→66vh 无效——**1400px 宽度断点里有自己的
     `--compass-size: min(440px,60vw,66vh)`**，文件位置更靠后直接覆盖根定义；
     50vh 的 1000px 断点同理。**改 :root 变量必须检查响应式区每个断点的同名覆盖**；
  3. 最终方案：max-height 媒体块放在**所有宽度断点之后**（430px 断点后）——
     `@media (max-height:800px){ :root{--compass-size:...62vh} }` 只在矮视口缩罗盘，
     大屏 1080p 罗盘仍 720px 不受影响；max-height:720px 再收紧 gap/字号。
- 验证数值：1264×625 视口 gap=+6px（文字底 113 / 罗盘顶 119）；
  390×844 手机 gap=154px，标题自然折两行居中。
- 教训：agent-browser **跨 Bash 调用会掉线**（再次复现），open+eval+screenshot 必须同命令链；
  file:// 有 CSS 缓存，验证改动要加 ?v=N 查询参数或读 getComputedStyle 确认生效值。

## 子页面 Index 弹层顺序统一 + 页尾上/下一项目导航（Staged 4 页 + root LLM）
- 用户指定路径：Staged\{ucl,ai-commerce,interior-ai,global-relay} + root\work-LLM_to_Construction。
  **Staged 4 文件 = 昨天 topbar 改版的副本**（grep 确认有 view-link），在其上继续编辑。
- **Index 弹层新顺序**（=首页 indexPanel：ucl→commerce→LLM→interior→relay，编号 01-05 重排；
  各卡保留自己的缩略图：ucl STIG_00 / commerce EC-0 / LLM BRIDGE_00 / interior ARC-1 / relay City-NY）。
  四个 Staged 文件的弹层块逐字节相同，同一 old_string 五页通用。
- **页尾导航链（循环闭合）**，统一双 span 结构 `<span>Previous|Next Project</span>项目名`：
  ucl↔commerce↔LLM↔interior↔relay↔ucl。
  - 例外：ucl 页 .minimal-link 是独立风格（中文+尾↗+hover 边框位移，无 span 前置标签样式），
    保留其格式改为「上一项目 · XXX ↗」两行；
  - commerce 页原「工作台 Demo」链接被替换（正文 2569 行仍有「打开工作台 Demo」入口，无损失）；
  - ucl 页原「返回首页」被替换（topbar brand 仍可回首页）。
- **EBUSY 复现**：commerce io 块首报 EBUSY，同文件 footer 编辑成功；grep 确认 footer 落盘后隔命令重试 io 块即过。
- 验证：五页 grep（data-page 顺序 + minimal-link）全对；浏览器实测 LLM 页弹层 01-05 顺序与
  当前页 outline 高亮正常、页尾 PREVIOUS AI电商素材生成平台 / NEXT 商业空间视觉设计 渲染正常。
- **未决事项（已提醒用户）**：root 目录的 4 个 work-*.html 未同步本次改动（Index 顺序/页尾仍是旧版），
  与 Staged 版本出现分叉；用户确认后可一键同步。

## 【纠正】目标文件搞错：Staged 是废弃副本，root 才是真身
- 用户反馈：「你更新了错误的子页面。我把错误的子页面都放入了 Superseded\Overdesign」。
- **目录语义澄清**：Staged/ 的 4 个 work-*.html 是废弃版本（用户已移入 Superseded\Overdesign\），
  **根目录才是 GitHub Pages 部署的真身**。用户显式给的路径也可能是副本，改前先确认部署目录。
- 已把同一套改动（弹层五卡整块替换 + 页尾循环导航）重新应用到 root 4 页：
  work-ucl / work-ai-commerce / work-interior-ai / work-global-relay-conference（各 2 处 Edit）。
- grep 落盘验证：root 5 页（含上轮 LLM）io 顺序全部 ucl→commerce→LLM→interior→relay；
  页尾链 ucl(上relay/下commerce)→commerce(上ucl/下LLM)→LLM(上commerce/下interior)→
  interior(上LLM/下relay)→relay(上interior/下ucl)，循环闭合。Superseded 4 页不再动。

## index.html：首屏标题改为与罗盘同拍消隐
- 问题：首屏→projects 转场中，弧线在 is-morphing 起点就淡出（1.3s），而 .hero-intro
  没有独立淡出规则，要等 1150ms 后 in-portfolio 落地再随 compass-stage 淡 1.5s——文字晚约
  1.15s 开始、又多拖 0.2s，与罗盘「先后消失」。
- 修复（3 处）：① .hero-intro 加 `transition: opacity 1.3s var(--fade)`；
  ② 新增 `body.is-morphing .hero-intro, body.in-portfolio .hero-intro { opacity: 0 }`
  与弧线同组同拍；③ 回场镜像 `body:not(.is-morphing):not(.in-portfolio) .hero-intro
  { transition: opacity .9s var(--cubic) }` 与 compass-stage 回场 0.9s 同步。
- 验证（单次 eval 内连续采样，消除 CLI 往返误差）：is-morphing 后 intro 与 svg 的
  transitionDuration 同为 1.3s；105/307/508/709ms 四点 opacity 逐位相同
  （0.938/0.702/0.386/0.175）。--fade = cubic-bezier(0.35,0.2,0.3,1) 前段较陡，
  跨 CLI 采样容易采到淡出完成后，**测过渡曲线必须在单次 eval 里用 Promise 连续采样**。
