# 2026-09-06

## work-LLM_to_Construction.html 整页重构：「离散建造 PROMPT TO PRODUCTION」Case Study
- 用户给完整 9 章叙事脚本（首屏→命题→流程→核心决策→Mesh/Block 对照→参数候选→实体结果→实体执行→页尾收束），
  设计语言对齐 work-global-relay-conference.html（topbar/弹层/深蓝冷白/编辑式排版/整幅媒体卡/页脚）。
- 直接整页重写（Write 覆盖），保留：topbar（Projects • Index • 关于我）、Index 弹层（root 统一新序 + is-current）、
  页尾循环链（Previous commerce / Next interior）、reveal 滚动入场。
- 关键设计决策：
  - 媒体卡圆角全局 --radius: 8px（用户要求 ≤8px，弹层组件保持与其他页一致的 28/18px）；
  - 去掉 relay 的花瓣/光晕装饰（用户禁装饰性科技元素），hero 用深蓝渐变 + 白卡承载
    BRIDGE_MESH&BLOCK.jpg（4800x2700 正好 16:9，contain 不裁切）；
  - workflow.png 是黑底流程条 → 深色 .flow-strip 卡 contain 融合；
  - MeshAI.png 降为 .flow-input 右侧中等输入证据卡（用户要求不喧宾夺主）；
  - methodology.jpg（1.39 含 Opt1/2/3+图例）：第四章 diagram-card 整图 + figcaption 列比较变量；
    第六章 .opt-band 用 aspect 2/1 + object-position: 50% 100% 底部裁切恰好露出 Opt 标签行；
  - **选片纠错**：初版实体大图误用 BRIDGE (3)（实为构件特写）——看完图后改为 BRIDGE (5)（完整桥拱全景）；
    图组最终：m-a=BRIDGE(5) 大图 / m-b=BRIDGE(4) 竖幅构件细节 / m-c=BRIDGE(1) 展览现场。
- 视频自动播放兜底：autoplay 属性在部分环境不自启 + Safari muted 属性解析坑 →
  `video[autoplay].forEach(v => { v.muted = true; v.play().catch(()=>{}) })`，实测 paused=false 正常播放。
- 调试教训：
  - reveal 的 transform 未触发时会成为 offsetParent，**锚点定位必须用 getBoundingClientRect().top + scrollY**，
    不能用 offsetTop；`#discretization/#physical` 加 scroll-margin-top 顶栏避让；
  - smooth 滚动中再发 scrollBy 会 retarget 打断动画（截图停在页顶）——测试用 behavior:'instant'；
  - file:// 下 lazy 图片在 eval 时刻报 !complete 是正常态，滚动全页后再查 brokenImgs==[] 才有效；
  - 跨 Bash 浏览器掉线再次复现（本会话第 3 次），所有 open/eval/screenshot 单链完成。
- 验证全过：桌面 1440 九段截图正常；移动 390 无横向裁切（100vw 全幅节的 scrollWidth 差值被
  body overflow-x:hidden 吸收，触摸设备无此差值）；图片零损坏；Index 弹层 01-05 新序 + 当前页描边；
  hero 锚点跳转落点 top=68px 不被顶栏遮挡；页尾 Previous/Next 链接正确。

## 【纠正】首屏必须与 Global Relay 完全同构，不得自创
- 用户反馈：「你为什么更改我的首屏？不要更改首屏的任何信息，包含颜色。要按照 work-global-relay-conference.html，
  首屏就是logo，图片出现在首屏后的文字下方」。
- **子页首屏固定范式（以后所有子页照此，不要自创）**：
  - `.poster-hero` 深蓝海报（含双层白色高光渐变 + ::after 模糊光斑，颜色一并照抄 relay）；
  - `.poster-meta` 顶部单行小字；`.poster-symbol` 12 花瓣 + 中心 logo（brightness(0) invert(1) 反白）居中；
  - `.poster-footer` 底部超大标题（relay 是「AI Global」+ boxed「Relay」+ 年份）；
  - 长英文副题用 `.boxed-sm`（0.34em + 放宽字距 0.01em，避免继承 -0.08em 挤成一团）；
  - 主句/说明/图片放首屏后的 `.hero-statement`（居中 h1 + p + `.template-slot` 白卡图片），
    **图片永远不出现在首屏海报内**。
- LLM 页落地：meta=Text/Image→Mesh→Blocks→Fabrication；花瓣中心 logo 用 images/icon.png；
  标题=离散建造 + boxed Prompt to Production；hero-jump 跳链接删除。
- 已核对：桌面/移动首屏截图与 relay 同构，overflow 正常，无 .hero-core/.hero-jump/.hero-visual 残留。

## 【二轮】workflow.png 被 16:9 cover 裁切
- 用户反馈：workflow.png 在「从意图到构件」一节只显示上半 + 下边一小角，中间大片空白。
- 根因：图原 1173×349（3.36:1 横向长条），但 `.campaign-feature` 全局 `aspect-ratio:16/9 + object-fit:cover`，
  cover 强行垂直放大把上下全切掉，露出中部留白。
- 修法（仅动单图，不影响其他 campaign-feature 的 16:9 cover 默认）：
  - CSS 加 `.campaign-feature--flow { aspect-ratio:auto; background:var(--paper); }`
    和 `.campaign-feature--flow img { height:auto; max-height:none; object-fit:contain; }`；
  - workflow 那张 `<figure>` 加 modifier 类 `campaign-feature campaign-feature--flow`。
- 验证：容器 1163×346，img object-fit=contain，整图完整显示，无横向溢出。
- 适用场景：任何原图比例明显横长（如 workflow、流程图、长 banner）用 --flow；其它 16:9 风景照仍 cover。

## work-ucl.html 首屏按钮下移到下滑后的文字下方
- 用户反馈：「首屏不应该放置按钮。应该像 work-LLM_to_Construction.html 一样，按钮放在首屏向下滑以后的文字下方」。
- 改动（3 处，文件本身即可，无共享样式影响）：
  1. 从首屏 `.case-footer` 里删掉 `.hero-actions`（含「查看动作验证 ↓」「查看研发过程 ↓」两个 minimal-link）；
  2. 新增 `.hero-quicknav` 样式（沿用 LLM 页面写法，白底胶囊 + 配色锚点 `--green`，
     阴影 rgba(34,58,51,*) 适配 UCL 蓝绿）；
  3. 在 `<header class="product-layer-head">` 内、说明段 `<p>` 之后插入：
     ```html
     <div class="hero-quicknav" aria-label="项目入口">
       <a href="#motion"><span>01</span>查看研发过程</a>
       <a href="#validation-reel"><span>02</span>查看动作验证</a>
     </div>
     ```
- 验证：首屏 `.case-footer` 只剩 case-title+case-summary；product-layer-head 居中布局下按钮水平居中；
  锚点 #motion、#validation-reel 在页面内已存在；移动 680 段 gap/padding/fz 同步缩小；
  桌面无横向溢出。
- **范式确立**：所有 case-landing 子页的「查看xxx」类跳转按钮都放进下滑后的第一个文字块下方，
  不再放回首屏。LLM + UCL 已统一。

## index.html「关于我」改为页尾直接展示完整内容
- 用户反馈：「修改 LINE 755-766，不要只放一个 button，而是直接展示 About Overlay 全部内容；
  保持从项目页进入关于我的动效；只修改 index，不要改 work 子页面」。
- 改动范围：**仅 index.html + about-overlay.js/css**；五个 work 子页面曾因误操作被改为 `href="index.html#about"`，
  已按用户要求全部恢复为原来的 `data-about-open` overlay 触发模式。
- index.html 具体改动：
  1. `#footerStage` 内由单一按钮改为 `.footer-mount`，about-overlay.js 以 embedded 模式注入完整 About 内容；
  2. 顶栏 `#aboutBtnTop` 恢复为进入 footer stage（保留 about-direct 过渡动效），移除 `data-about-open`；
  3. 增加 `#about` hash 自动进入 footer stage 的逻辑；
  4. 内联 CSS 调整 `.footer-mount` 滚动容器、copyright 位置、避免 sticky 冲突。
- 关键修复（右侧图表动画）：
  - 根因：embedded 模式下 `renderChart()` 只在 `open()` 中调用，而 `open()` 在 embedded 时直接 return，
    导致 SVG 为空；
  - 修法：在 `build()` 的 embedded 分支末尾直接调用 `renderChart()`；
  - 动画触发时机：将 `.footer-stage .ln-*` 动画选择器改为 `body.in-footer .footer-stage .ln-*`，
    确保进入页尾时才开始播放淡入/生长/弹出动画。
- 验证：桌面 1440 页尾完整展示双栏 + L11 时间轴动画；#about hash 直达自动进入 footer；
  横向溢出为 0；子页面「关于我」仍为当前页 overlay。
